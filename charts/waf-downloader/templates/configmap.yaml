apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "waf-downloader.fullname" . }}
  namespace: {{ include "waf-downloader.fullname" . }}
data:
  CHUNK_SIZE: {{ .Values.chunkSize | quote }}
  CLOUDFLARE_ZONE_ID: {{ .Values.cloudflare.zoneId | quote }}
